{
  "name": "tokenized-scrypt",
  "version": "0.1.0",
  "scripts": {
    "test": "mocha -r ts-node/register tests/*.scrypttest.*  --reporter spec --timeout 600000",
    "single-test": "mocha --reporter spec --timeout 120000"
  },
  "author": "Curtis Ellis",
  "dependencies": {
    "chai": "^4.3.4",
    "minimist": "^1.2.5",
    "mocha": "^9.0.3",
    "scryptlib": "*"
  },
  "directories": {
    "test": "tests"
  },
  "description": ""
}
